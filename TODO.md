# TODO

- [ ] Add a root (`/`) route that always renders the login page (or redirects to `/auth/login`).
- [ ] Ensure this behavior works regardless of authentication state.
- [ ] Run the app (smoke test) to confirm `/` shows login.

